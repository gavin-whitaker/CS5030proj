#include "distance.h"

// Skeleton-only implementation. Kept compilable; no logic yet.
double euclidean_distance(const std::array<double, 3> &a,
                           const std::array<double, 3> &b) {
  (void)a;
  (void)b;
  // TODO: compute sqrt(sum((a_i - b_i)^2)).
  return 0.0;
}

