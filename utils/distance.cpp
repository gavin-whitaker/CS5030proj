// distance.cpp — Implementation of distance functions for K-Means clustering
// TODO: implement euclidean_distance_sq and nearest_centroid

#include "distance.h"
#include <cmath>
#include <limits>

double euclidean_distance_sq(const Point &a, const Point &b) {
    // TODO: compute sum of squared coordinate differences
    return 0.0;
}

int nearest_centroid(const Point &p, const std::vector<Point> &centroids) {
    // TODO: iterate centroids and return index of the closest one
    return 0;
}
