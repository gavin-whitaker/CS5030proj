#include "validate.h"

// Skeleton-only placeholder. The Python validator is also required by the project.
bool validate_outputs(const std::string &serial_csv,
                       const std::string &parallel_csv,
                       double tolerance) {
  (void)serial_csv;
  (void)parallel_csv;
  (void)tolerance;
  // TODO: parse both CSVs and compare cluster assignments.
  return true;
}

