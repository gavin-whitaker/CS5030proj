// cuda/main.cu — Entry point for the CUDA K-Means implementation
// TODO: parse arguments, load data, call kmeans_cuda, write results

#include <iostream>
#include "../utils/io.h"
#include "../utils/kmeans_common.h"
#include "kmeans_cuda.cuh"

int main(int argc, char *argv[]) {
    // TODO: parse command-line arguments (input file, k, max_iter, output file)
    // TODO: read_points(...)
    // TODO: kmeans_cuda(...)
    // TODO: write_results(...)
    std::cout << "CUDA K-Means: not yet implemented." << std::endl;
    return 0;
}
