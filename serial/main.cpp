// serial/main.cpp — Entry point for the serial K-Means implementation
// TODO: parse arguments, load data, call kmeans_serial, write results

#include <iostream>
#include "../utils/io.h"
#include "../utils/kmeans_common.h"
#include "kmeans_serial.h"

int main(int argc, char *argv[]) {
    // TODO: parse command-line arguments (input file, k, max_iter, output file)
    // TODO: read_points(...)
    // TODO: kmeans_serial(...)
    // TODO: write_results(...)
    std::cout << "Serial K-Means: not yet implemented." << std::endl;
    return 0;
}
