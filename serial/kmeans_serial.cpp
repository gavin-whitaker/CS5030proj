// kmeans_serial.cpp — Serial K-Means clustering implementation
// TODO: implement the serial K-Means algorithm

#include "kmeans_serial.h"
#include "../utils/distance.h"

std::vector<Point> kmeans_serial(std::vector<Point> &points,
                                 const KMeansParams &params) {
    // TODO: initialize centroids, iterate assign/update steps until convergence
    std::vector<Point> centroids;
    return centroids;
}
